import React from "react";

const About = () => (
  <div className="text-center">
    <h2>About This Project</h2>
    <p>This AI-based web app helps farmers detect plant diseases by analyzing leaf images. It is built with Flask (backend) and React (frontend).</p>
  </div>
);

export default About;
